# SDN-Lab-6
Build shortest path routing based on L2swtich project

Stop L2switch functionalities
```
bundle:list | grep -i l2switch
bundle:stop <BUNDLE_IDs>
```
```
bundle:list | grep -i packethandler
bundle:list | grep -i arphandler-impl
bundle:stop <BUNDLE_IDs>
```

```
sudo snap install intellij-idea --classic
```
